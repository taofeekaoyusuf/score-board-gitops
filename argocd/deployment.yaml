apiVersion: apps/v1
kind: Deployment
metadata:
  name: score-board-app
  namespace: production
  labels:
    app: score-board-app
spec:
  replicas: 2
  selector:
    matchLabels:
      app: score-board-app
  template:
    metadata:
      labels:
        app: score-board-app
    spec:
      containers:
      - name: score-board-app
        image: dhackbility/score-board-app:jenkins-score-board-app-7
        resources:
          requests:
            cpu: "100m"
            memory: "128Mi"
          limits:
            cpu: "250m"
            memory: "256Mi"
        ports:
        - containerPort: 80
---
apiVersion: v1
kind: Service
metadata:
  name: score-board-app-service
  namespace: production
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 80
  selector:
    app: score-board-app